inkomoko:
  target: dev
  outputs:
    dev:
      type: clickhouse
      schema: analytics
      host: "{{ env_var('CLICKHOUSE_HOST', 'clickhouse') }}"
      port: "{{ env_var('CLICKHOUSE_PORT', '8123') | as_number }}"
      user: "{{ env_var('CLICKHOUSE_USER', 'inkomoko_admin') }}"
      password: "{{ env_var('CLICKHOUSE_PASSWORD', 'inkomoko_password') }}"
      secure: False
